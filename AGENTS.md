# AGENTS.md

本文件用于约束 AI Agent 在 s2u 仓库中的修改。s2u 是一个将普通网址或 URL Scheme 转换为可分享 HTTP 跳转链接的小型工具，核心风险集中在链接协议、Hash 编解码、本地历史、导入导出与 GitHub Gist 同步。

## 1. 修改前必读

按顺序读取：

1. 当前 Issue、PR 描述和验收标准；
2. `README.md`；
3. 本文件；
4. `src/domain/linkCodec.js`、相关 composable、测试和同类实现。

开始修改前先确认：

- 本次只解决一个主要问题；
- 是否影响新旧链接兼容；
- 是否影响历史记录、删除标记、导入导出或 Gist 同步；
- 是否引入新的协议、安全头、存储位置或外部请求。

## 2. 项目边界

技术与数据边界：

- Vue 3 Composition API 与 `<script setup>`；
- Vite 构建；
- `localStorage` 保存历史和普通配置；
- `sessionStorage` 保存 GitHub Token；
- GitHub Gist 作为可选同步端；
- 目标链接写入 URL Hash，不发送到静态托管服务器；
- `linkCodec` 是链接规范化、协议校验和 Hash 编解码的统一入口。

禁止在组件中建立第二套链接解析、历史合并、Token 存储或同步机制。

## 3. 实现规则

- 不直接推送 `master`；
- 使用 `feat/*`、`fix/*`、`refactor/*`、`test/*`、`docs/*` 或 `chore/*` 分支；
- 一个 PR 只解决一个主要问题；
- 不混合无关重构、依赖升级和全局格式化；
- 优先复用现有 composable、domain 工具和样式；
- 不为小需求引入新的状态管理、后端服务或大型依赖；
- 提交说明使用中文；
- 默认 squash merge，合并后删除功能分支。

## 4. 安全边界

公开跳转链路不得允许能够执行脚本或访问本地资源的协议，包括但不限于：

- `javascript:`；
- `data:`；
- `vbscript:`；
- `file:`；
- `blob:`。

普通网页协议与明确支持的自定义应用 Scheme 可以继续使用，但必须经过统一规范化和测试。

其他要求：

- GitHub Token 只能保存在当前标签页的 `sessionStorage`；
- 不将 Token、Cookie、Gist 内容或接口响应写入日志、截图、Issue、PR 或 Artifact；
- Gist 拉取失败时不得继续推送覆盖远端；
- 同步必须保持先拉取合并、再串行推送；
- 删除必须保留 tombstone，避免旧设备恢复已删除记录；
- 导入文件必须校验大小、版本和数据结构；
- 外部 URL、导入文件和 Gist 响应一律视为不可信输入；
- CSP、Referrer-Policy、防嵌入和权限限制不得在无说明的情况下放宽。

## 5. 测试要求

以下改动必须补充或更新测试：

- URL、URL Scheme、Hash、百分号、查询参数和 Unicode 编解码；
- 旧版链接兼容；
- 危险协议拒绝；
- 历史规范化、合并、排序和 tombstone；
- 导入版本、大小和无效记录；
- Gist 拉取失败、超时、取消和串行推送；
- 快捷键、输入法组合状态和数据存储边界。

完成前实际执行：

```bash
npm run lint
npm test
npm run build

# 或
npm run check
```

PR 必须写明实际结果、未执行项和剩余风险。不得把“应该可以”写成已验证。

## 6. UI 与动效

- 优先复用现有视觉变量、组件结构和交互模式；
- 长链接不得撑开网格、按钮或页面；
- 动画不得导致内容先完整展开、缩回再展开；
- 支持 `prefers-reduced-motion`；
- 快捷键不得覆盖浏览器或系统组合键；
- 修改响应式布局时同时检查桌面端和移动端。

## 7. 合并条件

只有同时满足以下条件才能合并：

- CI 通过；
- 分支无冲突；
- 无未解决审查线程；
- 变更范围与 PR 描述一致；
- 实际验证覆盖相关失败路径；
- 没有放宽危险协议、Token 存储或安全响应头；
- 临时诊断代码和无长期价值的工作流已删除或关闭而不合并。

继续维护该项目时，可在独立治理 PR 中评估将默认分支从 `master` 迁移为 `main`，不得在普通功能 PR 中顺带迁移。